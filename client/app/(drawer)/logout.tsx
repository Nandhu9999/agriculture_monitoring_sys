import { Button, View } from "react-native";

export default function LogoutPage() {
  return (
    <View>
      <View style={{ flex: 1 }}>
        <Button title="Logout" onPress={() => {}} />
      </View>
    </View>
  );
}
